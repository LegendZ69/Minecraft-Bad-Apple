# v1.1.0 — provenance and cloud verification

Mod version **1.1.0** targets **Minecraft Java Edition 1.21.1**, with Java 21,
Fabric Loader 0.16.5 or newer, and Fabric API for Minecraft 1.21.1. It is not a
port to another Minecraft version.

This version adds source provenance, playback fixes, and enforced release
verification. [Run 34179488804](https://github.com/LegendZ69/Minecraft-Bad-Apple/actions/runs/34179488804)
completed both synthetic checks and the full original animation with the exact
mod JAR in Fabric's non-development production namespace. Each publication must
repeat those gates. The release tag,
[build-info.json](https://github.com/LegendZ69/Minecraft-Bad-Apple/releases/download/v1.1.0/build-info.json),
and packaged reports identify the final revision and its gate results; the
completed run documented here is not a claim that a later workflow has finished.

## Changes from v1.0.0

- Explicit source selection for the official label upload, original NicoVideo
  animation, and user-linked YouTube reference; no silent substitution.
- Recorded source provenance and a bounded public reference-access report.
- Exhaustive archive verification against the local source, with a saved report.
- Verified conversion of the original NicoVideo animation: all 6,573 decoded
  frames, relative timestamps, and normalized audio match the acquired source.
- Clearly synthetic video/audio fixtures for repeatable conversion and runtime
  testing without redistributing the original animation or recording.
- An isolated Minecraft client smoke-test harness and versioned release records.
- Add an isolated production-namespace test using Fabric's official
  [production run tasks](https://docs.fabricmc.net/develop/loom/production-run-tasks),
  binding both synthetic and full-original tests to the released mod JAR's
  SHA-256. This is not an authenticated Minecraft Launcher test.
- Replace the inherited native `Clip` output with a `SourceDataLine` adapter
  requesting a 20 ms buffer. Cloud Linux checks also set
  `PULSE_LATENCY_MSEC=20`; see the README for the tested launch configuration.
- Correct stale audio-clock readings after backward seeks, restart, and looping,
  and preserve elapsed time when an unresponsive device falls back to silent
  playback. Timestamp native audio queries after they return so blocking device
  reads do not falsely trigger silent fallback.
- Decode PNGs through an expandable native-buffer path instead of copying a
  complete encoded frame onto LWJGL's small native stack. This fixes the
  inherited v1.0.0 failure on larger PNG frames; decoder termination is now
  surfaced instead of silently leaving a frozen texture.
- Add full-run texture freshness/progress checks so an unchanged old texture
  cannot pass solely because its stored GPU pixels still match that old frame.
  Deterministic, synthetic controls/audio, and full-length original-media checks
  have passed, including exact-JAR production-namespace verification.
- Make restart verification compare one coherent playback-position sample with
  elapsed command-dispatch time, tolerating bounded scheduler delays without
  accepting stale jumps or a frozen clock.

## Native audio buffering diagnosis

The baseline's native `Clip` path showed abnormal buffer/clock behavior in cloud
Linux tests. OpenJDK's
[DirectAudioDevice](https://github.com/openjdk/jdk21u/blob/master/src/java.desktop/share/classes/com/sun/media/sound/DirectAudioDevice.java)
requests a 1,000 ms clip buffer. Its
[Linux ALSA backend](https://github.com/openjdk/jdk21u/blob/master/src/java.desktop/linux/native/libjsound/PLATFORM_API_LinuxOS_ALSA_PCM.c)
estimates position from buffer availability, while
the [ALSA PulseAudio plugin](https://github.com/alsa-project/alsa-plugins/blob/master/pulse/pcm_pulse.c)
clamps writable space used to advance its simulated ring buffer. The completed
[four-case probe](https://github.com/LegendZ69/Minecraft-Bad-Apple/actions/runs/34177059058)
supports the low-latency configuration: only the 20 ms `SourceDataLine` plus
`PULSE_LATENCY_MSEC=20` produced the intended nine seconds of non-silent output
in both channels, with 97.78% of audible 100 ms windows within 440 ± 20 Hz.
The other cases showed start/seek lag and shortened captured output. Four
successful job exits did not mean four correctly behaving configurations.

## Verification record

| Check | Result |
| --- | --- |
| Completed exact-JAR production run | [Run 34179488804](https://github.com/LegendZ69/Minecraft-Bad-Apple/actions/runs/34179488804), commit `ca380ac5be96533497f6c7cad7f798a0d40a10b7`: whole run succeeded |
| Python and Java tests | 110 Python and 38 Java tests passed in that run |
| Fabric compile/remap and installable JAR | Passed, including JAR reproducibility |
| Original NicoVideo source acquisition and conversion | Passed and repeated in that run |
| Exhaustive source-to-archive verification | Passed: 512 × 384, 6,573 frames, 3,876,913,152 RGB24 bytes, every relative timestamp, 219.100-second duration, and 10,516,800 normalized stereo PCM sample frames |
| Exact remapped mod JAR in Fabric's production-test launcher | Both synthetic controls/GPU/strict stereo and full-original playback passed; `developmentEnvironment=false`, namespace `intermediary`, same loaded JAR SHA-256 |
| Authenticated Minecraft Launcher profile installation | Not exercised; separate from Fabric's production-test launcher |
| 219-second synthetic source conversion and exhaustive comparison | Passed; this is a conversion stress test, not a 219-second synthetic Minecraft playback test |
| Screenshots, runtime reports, and logs | Allowlisted evidence is packaged with file digests; final asset identities are recorded in build-info.json and SHA256SUMS |
| Native buffer probe and low-latency adapter | Probe complete; integrated synthetic Minecraft controls/stereo checks passed for the selected configuration |
| Equivalence with official/user YouTube references | Not verified; both YouTube URLs required sign-in/bot confirmation |
| Full original-source archive playback, production runtime | Passed: 219.1 seconds of media in 219.2220 seconds, final frame 6,572, 73 assertions, no audio fallback |
| Large encoded-PNG regression | Passed: a 787,004-byte PNG decoded 16 times, 3,145,728 exact pixels, native stack pointer preserved |

### Publication gates and exact build identity

The final build calls the packager with `--require-original --require-production`.
It rejects publication unless **both** synthetic controls/GPU/stereo verification
and uninterrupted full-original playback report a non-development,
`intermediary`-namespace runtime loading the exact release JAR SHA-256. The
original gate also binds the acquired source, exhaustive conversion report,
runtime archive, texture freshness/progress checks, and sustained stereo output.
Both gates completed in run 34179488804 and must pass again for the final
published revision. CI uses Bash pipeline-failure propagation so logging through
`tee` cannot hide a failed command. The publisher also rechecks that the verified
commit is still the latest `main` commit before publishing.

Periodic playback evidence snapshots the renderer's already-updated timeline
before independent native audio diagnostics. A cloud run exposed a 1.17-second
blocking diagnostic query that incorrectly compared a previously captured frame
with a later clock reading. The sampling correction retains the one-second
displayed-frame age limit, source progress checks, and complete-playthrough
requirement; later raw audio readings keep their own observation timestamps.
This changes the smoke harness, not the installable player JAR.

Publication uses the configured GitHub token and lets the release API enforce
its permissions. Repository metadata's `permissions.push` field is not used as
a substitute for the installation token's release capability; actual API
authentication and permission errors stop publication without retries or
credential changes. The verified current version is published before the
historical baseline. GitHub may require additional `Workflows: write` permission
for a historical target whose workflow files differ from the default branch;
the Actions `GITHUB_TOKEN` cannot receive that permission. See GitHub's
[release permission contract](https://docs.github.com/en/rest/releases/releases#create-a-release).
The baseline's separate access requirement does not invalidate the current
version's completed tests or publication verification.

`build-info.json` records the production JAR revision/digest, both runtime
bindings, source/verification evidence, and separate portable-source digests.
`SHA256SUMS` covers the immutable release assets. Read those packaged results for
the final publication, rather than treating historical measurements below as a
new measurement of a later build. A checksum detects changes; it is not a
signature or proof of media redistribution rights.

The completed production run binds these exact files. A later published run's
hashes must be read from its own `build-info.json` and comparison report.

| File in run 34179488804 | SHA-256 |
| --- | --- |
| Loaded mod JAR, synthetic and original tests | `4e839f447e295ba6844f999a6fa46011ec2965c84ce9691dfb8b0945d8df2f5f` |
| Downloaded original source | `b044ff73cd9aa00ae0a16e2d7e2290d64da3caa2edbfc88b05eff8344a89ca6b` |

The source-to-archive result proves equality with that acquired local file, not
with either inaccessible YouTube upload. The platform's metadata duration is
219 seconds; the decoded video/archive duration is 219.100 seconds. PCM equality
is measured after the converter's documented 48 kHz stereo signed-16-bit
alignment, resampling, padding, and trimming. It is not a claim that compressed
source audio bytes were retained unchanged. See
[reference sources](../REFERENCE_SOURCES.md) for provenance and interpretation.

### What the complete playthrough actually displayed

| Production measurement in run 34179488804 | Result |
| --- | --- |
| Distinct source frames uploaded during playback | 6,001 of 6,573 (91.2977%); 572 skipped |
| Mean world-render callbacks | 29.7735 per second; **not every source frame displayed or guaranteed 30 FPS** |
| Periodic GPU readback | 45 samples, 8,847,360 pixels exactly matching the frames actually uploaded |
| Maximum sampled uploaded-frame lag | 58.585 ms |
| Captured non-silent stereo output | 218.3 seconds in each channel within a 262.612-second diagnostic recording |
| Peak sampled Java heap | 1,156,530,464 bytes with a 3 GiB maximum |

All source frames were preserved in the archive; **not all were displayed** in
this runtime test. The evidence does not isolate the cause of the observed
rendering throughput, so it must not be attributed to software rendering alone.
GPU equality concerns sampled uploaded textures, not every source frame or the
perspective-transformed final screen. Audio-clock agreement is not independent
physical audio/video synchronization evidence.

Historical comparison: the first complete development-runtime run,
[34178311059](https://github.com/LegendZ69/Minecraft-Bad-Apple/actions/runs/34178311059),
passed 101 Python/38 Java tests and displayed 2,295 frames (34.9%), skipping
4,278 at 10.74 render callbacks/s. Those earlier figures describe that run,
not the production result above; the evidence does not isolate the cause of
the throughput difference.

The conversion check and synthetic tests establish only the behavior they
actually measure. Virtual-device stereo capture is not physical-speaker testing.
Complete in-game presentation of every original frame, performance on every
computer, multiplayer playback, and shader compatibility are not established.
This is a client-rendered screen, not a wall of changing world blocks.
`runSmokeClient` runs the actual Minecraft 1.21.1 client with Fabric's named-class
development runtime. The additional remapped-JAR check uses the official Fabric
production-test launcher in an isolated Loom 1.10.5 / Gradle 8.12 project; it does
not upgrade the main Loom 1.7.4 / Gradle 8.8 build. Both exact-JAR production
tests completed in the recorded run and are mandatory publication gates.
Neither route tests account sign-in or a user's authenticated Minecraft Launcher
profile. Use a normal licensed Minecraft installation and Fabric for the
installation steps below.

## Download and run

1. Download `minecraft-bad-apple-1.1.0.jar` from the
   [v1.1.0 release](https://github.com/LegendZ69/Minecraft-Bad-Apple/releases/tag/v1.1.0)
   and put it in the Minecraft instance's `mods` folder alongside Fabric API.
   Install the normal mod JAR, not a sources JAR or test artifact.
2. Download the matching source/tools bundle or check out tag `v1.1.0`. With
   Python 3.10+, FFmpeg, and ffprobe available, prepare and verify an authorized
   local reference:

   ```sh
   python tools/prepare_video.py "reference.mp4" --output bad_apple.bapple
   python tools/verify_archive.py bad_apple.bapple --source "reference.mp4" --report verification.json
   ```

3. Copy `bad_apple.bapple` to `<instance>/badapple/bad_apple.bapple`. Start
   Minecraft Java 1.21.1 with Fabric, join a world, and run `/badapple play`.
4. Use `/badapple status` to inspect playback and `/badapple place native` for
   one source pixel per block of screen area. Pause with `/badapple pause`.

For explicit official-source download options, access limitations, and media
rights boundaries, read [reference sources](../REFERENCE_SOURCES.md). Original
animation/audio is not an asset in this public release; a synthetic fixture is
not the requested Bad Apple!! animation. Evidence may include selected in-game
screenshots and complete metadata/measurement reports, not the full source video
or its converted playback archive.

## Reproduce the cloud checks

Use JDK 21, Gradle 8.8, Python 3.10+, FFmpeg, and ffprobe. A graphical Minecraft
client also needs an OpenGL-capable display; Linux cloud runs use a virtual
display and software rendering configured by CI.

```sh
gradle build
python -m unittest discover -s tests -v
python tools/generate_fixture.py --output build/synthetic.mkv
python tools/prepare_video.py build/synthetic.mkv --output build/smoke-game/badapple/smoke.bapple
python tools/verify_archive.py build/smoke-game/badapple/smoke.bapple --source build/synthetic.mkv --report build/archive-verification.json
gradle runSmokeClient
```

The smoke harness is isolated in `src/smoke`; it is not part of the installable
mod. The fixture path above is intentional. Run the client under the display
setup used in the workflow, and inspect its saved reports and logs rather than
inferring success merely from a process having started. Preserve the source,
archive, verification report, and runtime evidence together when reproducing a
result.

For the exact-JAR production checks, follow `.github/workflows/build.yml`: it
builds the separate remapped smoke harness, prepares fresh isolated game
directories, verifies the Gradle 8.12 download, and runs `prodSmokeClient` and
`prodReferenceClient` in `tools/production-smoke`. The latter replaces a second
development-mode full playthrough; it uses the same installable release JAR.
