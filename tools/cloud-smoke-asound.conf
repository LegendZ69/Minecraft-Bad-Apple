# Isolated CI configuration: Java Sound's default ALSA device targets PulseAudio.
# Point ALSA_CONFIG_PATH here; do not overwrite a user's global audio settings.
</usr/share/alsa/alsa.conf>
pcm.!default {
    type pulse
}
ctl.!default {
    type pulse
}
